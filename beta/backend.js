/* Browser-side backend layer for the static beta build. It persists the workspace locally. */
window.FLBackend=(()=>{const KEY='fl_code_beta_workspace_v1';const clone=x=>JSON.parse(JSON.stringify(x));const initial=[{id:'root',name:'FL-Code-Beta',type:'folder',parent:null,hidden:false},{id:'src',name:'src',type:'folder',parent:'root',hidden:false},{id:'mainjs',name:'main.js',type:'file',parent:'src',hidden:false,language:'JavaScript',content:'console.log("Hello from FL Code Beta");\n'},{id:'readme',name:'README.md',type:'file',parent:'root',hidden:false,language:'Markdown',content:'# FL Code Beta\n\nWelcome to the beta workspace.\n'},{id:'config',name:'config.json',type:'file',parent:'root',hidden:false,language:'JSON',content:'{\n  "name": "FL Code Beta"\n}\n'},{id:'api',name:'api',type:'package',parent:'root',hidden:false},{id:'db',name:'database',type:'network',parent:'root',hidden:false}];function load(){try{const x=JSON.parse(localStorage.getItem(KEY));return Array.isArray(x)?x:clone(initial)}catch{return clone(initial)}}function save(tree){localStorage.setItem(KEY,JSON.stringify(tree));return tree}let tree=load();return{all:()=>clone(tree),save:t=>(tree=save(t),clone(tree)),reset:()=>{tree=clone(initial);save(tree);return clone(tree)},create(item){tree.push(item);save(tree);return clone(item)},update(id,patch){const i=tree.findIndex(x=>x.id===id);if(i<0)return null;tree[i]={...tree[i],...patch};save(tree);return clone(tree[i])},remove(id){const ids=new Set([id]);let changed=true;while(changed){changed=false;tree.forEach(x=>{if(ids.has(x.parent)&&!ids.has(x.id)){ids.add(x.id);changed=true}})}tree=tree.filter(x=>!ids.has(x.id));save(tree)},get(id){const x=tree.find(x=>x.id===id);return x?clone(x):null}}})();
